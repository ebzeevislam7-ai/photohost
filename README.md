# 📸 ASP.NET Core MVC Фотохостинг

Полнофункциональное веб-приложение для загрузки, хранения и управления фотографиями на ASP.NET Core MVC.

## ✨ Возможности

- ✅ Загрузка фотографий (.jpg, .png)
- ✅ Безопасное хранение файлов на сервере
- ✅ Просмотр всех загруженных фотографий
- ✅ Удаление фотографий
- ✅ Полная асинхронность (async/await)
- ✅ Dependency Injection
- ✅ Entity Framework Core с БД
- ✅ Красивый responsive UI

## 📁 Структура проекта

```
project/
├── Models/
│   └── Photo.cs                 # Модель Photo
├── Data/
│   └── AppDbContext.cs          # Entity Framework контекст
├── Services/
│   ├── IPhotoService.cs         # Интерфейс сервиса
│   └── PhotoService.cs          # Реализация сервиса
├── Controllers/
│   └── PhotosController.cs      # MVC контроллер
├── Views/
│   └── Photos/
│       └── Index.cshtml         # Razor представление
├── wwwroot/
│   └── uploads/                 # Папка для загруженных файлов
├── Program.cs                   # Точка входа
├── appsettings.json             # Конфиг БД
└── PhotoHost.csproj             # Проект файл
```

## 🚀 Установка и запуск

### 1. Требования
- .NET 8.0 SDK или выше
- SQL Server LocalDB (или измените ConnectionString)
- Visual Studio / VS Code

### 2. Создание миграций БД

```bash
dotnet tool install --global dotnet-ef
dotnet ef migrations add InitialCreate
dotnet ef database update
```

### 3. Запуск приложения

```bash
dotnet run
```

Приложение будет доступно на `https://localhost:5001` или `http://localhost:5000`

## 🔧 Конфигурация

### Изменение БД

В `appsettings.json` замените `ConnectionString`:

```json
// Для SQL Server
"DefaultConnection": "Server=YOUR_SERVER;Database=PhotoHostDb;User Id=sa;Password=YOUR_PASSWORD;"

// Для SQLite (более просто для разработки)
"DefaultConnection": "Data Source=photos.db"
```

Затем в `Program.cs` измените:
```csharp
options.UseSqlite(builder.Configuration.GetConnectionString("DefaultConnection"))
```

И добавьте пакет:
```bash
dotnet add package Microsoft.EntityFrameworkCore.Sqlite
```

### Путь для загрузок

По умолчанию файлы сохраняются в `wwwroot/uploads/`. Чтобы изменить, отредактируйте `PhotoService.cs`:

```csharp
private const string UploadFolder = "uploads";  // Измените на нужный путь
```

## 📝 Использование

1. **Открыть приложение**: Перейдите на главную страницу
2. **Загрузить фото**: Выберите файл (.jpg или .png) и нажмите "Загрузить"
3. **Просмотр**: Фотографии отобразятся в сетке ниже
4. **Удаление**: Нажмите кнопку "Удалить" на нужной фотографии

## 🔐 Безопасность

- ✅ Валидация расширений файлов (.jpg, .jpeg, .png)
- ✅ Генерация уникальных имён через Guid
- ✅ Обработка ошибок и логирование
- ✅ Асинхронная работа с файлами
- ✅ SQL-инъекции защищены (Entity Framework)

## 📚 Технологии

- **ASP.NET Core 8.0** - веб-фреймворк
- **Entity Framework Core** - ORM для БД
- **SQL Server** - база данных
- **Razor Pages** - UI шаблонизирование
- **Bootstrap-подобный CSS** - стили

## 🐛 Решение проблем

### Папка uploads не создаётся
- Убедитесь, что `wwwroot/` папка существует
- Проверьте права доступа к папке

### Ошибка подключения к БД
- Проверьте `appsettings.json`
- Убедитесь, что SQL Server запущен
- Запустите миграции: `dotnet ef database update`

### Файл не загружается
- Проверьте расширение (только .jpg, .png)
- Максимальный размер - проверьте конфиг в `Program.cs`

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано как полнофункциональный пример ASP.NET Core MVC приложения.
